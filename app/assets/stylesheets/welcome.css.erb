.col-centered{
    float: none;
    margin: 0 auto;
}

.username {
  font-weight: normal;
  font-size: 18px;
  line-height: 20px;
}

.intro {
  height: 800px;
  background-image: url('<%= asset_path('main-bg.png') %>');
  background-repeat: no-repeat;
  background-position: center; 
  background-size: 1120px 850px;
}

.intro label {
  font-size: 32px;
  text-align: center;
}

footer p {
  font-size: 18px;
  text-align: center;
}

